// Subpath import target: `@acme/math/double` resolves to this file via the
// shared extension fallback (no `exports` map needed for subpaths).
export function double(n: number): number {
  return n * 2;
}
