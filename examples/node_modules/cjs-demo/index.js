'use strict';
const { shout } = require('./helper');
const data = require('./data.json');
const sub = require('./sub');
module.exports = {
  greet(name) { return shout('hello ' + name); },
  version: data.version,
  subValue: sub.value,
};
